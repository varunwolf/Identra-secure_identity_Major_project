PORT=5000
MONGODB_URI=mongodb://127.0.0.1:27017/identity_system
JWT_SECRET=supersecret_jwt_key
JWT_EXPIRES_IN=7d

# SMTP settings (use a real provider in production)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=user@example.com
SMTP_PASS=change-me
MAIL_FROM=no-reply@example.com

# CORS (optional)
# CORS_ORIGIN=http://localhost:5173

